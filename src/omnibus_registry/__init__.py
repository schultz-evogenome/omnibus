"""Omnibus: a community registry of lab knowledge servers, served over the
Model Context Protocol. One repository, one manifest per node, checks on
every pull request, a router that fans a query out to registered nodes."""

__version__ = "0.1.0"
